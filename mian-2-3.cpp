#include <iostream>

using namespace std;

int main() {
	int number[] = {1,2,3,2,1};
	int length = 5;

	is_a_palindrome(number, length);
	sum_elements(number, length);
	sum_if_a_palindrome(number,length);
}